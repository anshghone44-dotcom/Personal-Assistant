import { motion } from 'framer-motion';
import { ArrowRight, Check, Clock3 } from 'lucide-react';
import type { Reminder } from '@/data/demo';

export function ReminderCard({ reminder, onDone, onLater }: { reminder: Reminder; onDone: (id: string) => void; onLater: (id: string) => void }) {
  return <motion.article layout data-testid={`card-reminder-${reminder.id}`} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, x: 20 }} className={`group flex items-center gap-4 border-b border-[hsl(var(--border)/.7)] py-5 ${reminder.state === 'completed' ? 'opacity-50' : ''}`}><button type="button" data-testid={`button-done-reminder-${reminder.id}`} onClick={() => onDone(reminder.id)} aria-label={`Mark ${reminder.title} done`} className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-full border transition-[background,border,color] ${reminder.state === 'completed' ? 'border-[hsl(var(--primary))] bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border-[hsl(var(--border))] text-transparent hover:border-[hsl(var(--primary))] hover:text-[hsl(var(--primary))]'}`}><Check size={13} /></button><div className="min-w-0 flex-1"><p className={`text-sm font-semibold ${reminder.state === 'completed' ? 'line-through' : ''}`}>{reminder.title}</p><p className="mt-1 truncate text-[11px] text-[hsl(var(--muted-foreground))]">{reminder.note}</p></div><div className="flex shrink-0 items-center gap-2"><span className="hidden items-center gap-1 font-mono text-[9px] text-[hsl(var(--muted-foreground))] sm:flex"><Clock3 size={11} /> {reminder.when}</span>{reminder.state !== 'completed' && <button type="button" data-testid={`button-later-reminder-${reminder.id}`} onClick={() => onLater(reminder.id)} aria-label={`Move ${reminder.title} later`} className="rounded-md p-2 text-[hsl(var(--muted-foreground))] transition-colors hover:bg-[hsl(var(--secondary))] hover:text-[hsl(var(--foreground))]"><ArrowRight size={14} /></button>}</div></motion.article>;
}