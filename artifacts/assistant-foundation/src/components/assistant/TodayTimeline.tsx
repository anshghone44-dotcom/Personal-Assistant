import { ArrowUpRight, CalendarDays, Check, Clock3 } from 'lucide-react';
import type { Reminder } from '@/data/demo';
import { events } from '@/data/demo';

export function TodayTimeline({ reminders, onReminder }: { reminders: Reminder[]; onReminder: (id: string) => void }) {
  const slots = [...events.slice(0, 3), ...reminders.filter((item) => item.state === 'today').map((item) => ({ id: item.id, title: item.title, time: item.when.split('·')[1]?.trim() || 'Today', end: '', location: 'Reminder', color: 'soft' as const, reminder: true }))].sort((a, b) => a.time.localeCompare(b.time));
  return <section><div className="mb-5 flex items-center justify-between"><div><p className="font-mono text-[9px] uppercase tracking-[.2em] text-[hsl(var(--muted-foreground))]">A small shape to the day</p><h2 className="mt-2 font-serif text-3xl tracking-[-.03em]">Today</h2></div><span className="font-mono text-[10px] text-[hsl(var(--muted-foreground))]">4 things</span></div><div className="relative space-y-1 border-l border-[hsl(var(--border))] pl-5">{slots.map((item) => <div key={item.id} data-testid={`timeline-event-${item.id}`} className="group relative flex items-center justify-between gap-4 py-3"><span className={`absolute -left-[25px] h-2.5 w-2.5 rounded-full border-2 border-[hsl(var(--background))] ${'reminder' in item ? 'bg-[hsl(var(--primary))]' : item.color === 'warm' ? 'bg-[hsl(var(--accent))]' : 'bg-[hsl(var(--primary))]'}`} /><div className="min-w-0"><p className="text-xs font-semibold">{item.title}</p><p className="mt-1 text-[11px] text-[hsl(var(--muted-foreground))]">{'reminder' in item ? 'A reminder' : `${item.location} · ${item.end}`}</p></div><div className="flex shrink-0 items-center gap-2">{'reminder' in item ? <button type="button" data-testid={`button-done-${item.id}`} onClick={() => onReminder(item.id)} className="rounded-md p-1.5 text-[hsl(var(--muted-foreground))] opacity-0 transition-[opacity,background,color] hover:bg-[hsl(var(--secondary))] hover:text-[hsl(var(--primary))] group-hover:opacity-100]"><Check size={14} /></button> : <Clock3 size={13} className="text-[hsl(var(--muted-foreground)/.7)]" />}<span className="font-mono text-[10px] text-[hsl(var(--muted-foreground))]">{item.time}</span></div></div>)}</div><button type="button" data-testid="button-open-timeline" className="mt-5 inline-flex items-center gap-1.5 text-[11px] font-semibold text-[hsl(var(--primary))]">Open day <ArrowUpRight size={13} /></button></section>;
}